#include<stdio.h>
#include<unistd.h>
int main(){//p1
   int n,p2;
   printf("enter a number:");
   scanf("%d",&n);
   p2=fork();
   int s=0,i;
   if(p2==0){
      for(i=1;i<n;i++){
	 if(n%i==0){
	    s=s+i;
	 }
      }
      if(s==n){
	 printf("perfect number\n");
      }
      else{
	 printf("not perfect number\n");
      }
   }
   else{
      if(n%2==0){
	 printf("even number");
      }
      else{
	 printf("odd number");
      }
   }
   printf("\n");
   return 0;
}
